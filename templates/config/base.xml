<project default="measure">

    <path id="classpath">
        <fileset dir="{{ project_path }}/lib/{{ checker }}">
            <include name="*.jar" />
        </fileset>
    </path>

    {% block content %}{% endblock %}

</project>
